name: first workflow

on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo "hello world"
      - name: print goodbye
        run: echo "Done - bye"
      
